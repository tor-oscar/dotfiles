= Dotfiles =

== List of dotfiles ==
If prepended with '-' it is missing but requested
- tmux
- vim test
- powerline test
- inputrc
- zsh
- gnome-terminal solarized test

== TODO ==
Vim: install .vimrc from dotfiles instead of from vim repo

== Install ==
HOME: The directory to install into
GNOME_TERMINAL_PROFILE: the gnome terminal profile to set colors in

== Dependencies ==
common: readlink, git
gnome-termina solarized: gconf (gconf-cli)

